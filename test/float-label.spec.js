import test from 'ava';
import {stub} from 'sinon';
import floatLabel from '../src/float-label.js';

test('this will always pass', t => {
    t.pass();
});

test('this will always fail', t => {
    t.fail();
});
